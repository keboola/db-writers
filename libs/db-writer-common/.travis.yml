sudo: false

services:
  - docker

install:
  - source ./tests/generate-ssh-keys.sh
  - docker pull waisbrot/wait
  - docker pull mysql:5.6
  - docker-compose build tests
  - docker-compose build sshproxy

script:
  - docker-compose run wait
  - docker-compose run tests
